<%- include('../shared/header') %>

<div class="min-h-screen flex items-center justify-center px-4">

  <div class="max-w-xl w-full bg-gray-100 shadow-lg rounded-lg p-6 text-center">

    <!-- Blog Image -->
    <% if (blog.image) { %>
      <img
        src="<%= blog.image %>"
        alt="Blog Image"
        class="w-full h-56 object-cover rounded mb-4"
      />
    <% } %>

    <h2 class="text-2xl font-bold mb-4 text-gray-800">
      <%= blog.title %>
    </h2>

    <p class="text-gray-600 mb-6 leading-relaxed">
      <%= blog.content %>
    </p>

    <a
      href="/blogs"
      class="inline-block text-blue-500 hover:text-blue-700 font-semibold"
    >
      ⬅ Back
    </a>

  </div>

</div>

<%- include('../shared/footer') %>
